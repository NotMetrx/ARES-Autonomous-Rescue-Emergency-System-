# ARES: Autonomous Rescue Emergency System
# Linux Edge / Embedded Drone Node Requirements (Ubuntu/Debian/JetPack)

# --- High-Performance Web & Async Server ---
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
uvloop>=0.19.0
httptools>=0.6.1
pydantic>=2.6.0
python-multipart>=0.0.9

# --- Security & JWT HS256 ---
PyJWT>=2.8.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.1.2

# --- Relational SQLite Persistence (WAL Mode) ---
aiosqlite>=0.20.0

# --- Scientific & AI Perception Stack (PyTorch D-FINE + Kalman 6-D + 3D Evasion) ---
numpy>=1.26.0
scipy>=1.12.0
torch>=2.2.0
# Headless OpenCV eliminates libGL.so / X11 GUI dependencies on embedded Linux
opencv-python-headless>=4.9.0.80

# --- Verification & Testing Suite ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0

# Note for lightweight CPU-only Linux edge deployment:
# pip install torch --index-url https://download.pytorch.org/whl/cpu
# Note for Jetson Orin / Nano (CUDA):
# PyTorch is pre-installed via NVIDIA JetPack wheel
